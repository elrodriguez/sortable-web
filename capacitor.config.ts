import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sortable-web',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
